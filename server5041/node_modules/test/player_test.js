

var Player = require('base/player');
var player = new Player();

//登录
player.wxLogin('12345678','','','','127.0.0.1',function (obj) {
    console.log(obj.formatterData());

    console.log("用户使用卡片");
    obj.useCard(function (success) {
        console.log(success?"扣除成功":"扣除失败");
    });
});