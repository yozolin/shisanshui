
var Record = require('base/record');
var BigRecord = require('base/big-record');
//var record = new Record();
var bigRecord = new BigRecord();

// record.info = "信息一";
// record.info0 = 'info0';
// record.info1 = 'info1';
// record.info2 = 'info2';
// record.info3 = 'info3';
// record.info8 = "{record:[{step1},{step2},{step3},{step4}]}";
// record.userId = 110000;
// record.gameIndex = 2;
// record.save(12,function (error,result) {
//     console.log(error == null?"保存成功":"保存失败");
// });
//
bigRecord.users = [123456,100002,100001];
bigRecord.save(function (error,result) {
   if (error == null) {
       bigRecord.scores = [23,-11,-12];
       bigRecord.save(function (error,result) {

       });
   }
   for (var i = 0; i < 4; i++) {
       var record = new Record();
       record.parentId = bigRecord.id;
       record.userId = 100000;
       record.gameIndex = 1;
       record.score = -5;
       record.info = '123456';//房间号
       record.save(bigRecord.id,function (error,result) {
           //console.log(error);
       });
   }
});

// BigRecord.getRecords(100005,function (error,result) {
//     if (error == null) {
//         console.log(JSON.stringify(result));
//     }
//     else {
//         console.log(error);
//     }
// });
